"""
# metacharacters
# . ^ $ * + ? { } [ ] \ | ( )
# . - any symbol
# ^ - matches at the beginning of the string 
# $ - matches at the end of the string
# * - quantifier, 0 or more repititions 
# + - quantifier, 1 or more repititions
# ? - quantifier, 0 or 1 repitiion
# {} - quantifier, allows to specify the exact amount of repititions
# [] - set of characters 
# \ - backslash, used for special sequences or escaping characters
# | - or, allows to check for 2 or more patterns 
# () - grouping 

translate


# метасимволы
# . ^ $ * + ? { } [ ] \ | ( )
# . - любой символ
# ^ - соответствует началу строки
# $ - соответствует концу строки
# * - квантификатор, 0 или более повторений
# + - квантификатор, 1 или более повторений
# ? - квантификатор, 0 или 1 повторение
# {} - квантификатор, позволяет указать точное количество повторений
# [] - набор символов
# \ - обратная косая черта, используется для специальных последовательностей или экранированных символов
# | - или, позволяет проверить наличие 2 или более шаблонов
# () - группировка
"""