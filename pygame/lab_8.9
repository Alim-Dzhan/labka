pygame.init() - это запускает процесс

fond - для текта 

fill закрашивает

 blit() для отрисовки 
 screen.blit

 количества кадров в секунду:
clock = pygame.time.Clock()    это объект, который помогает контролировать скорость игры

pygame.display.flip ( ) ¶
Обновите весь дисплей Surface на экране


pygame.sprite.Group()
Это создание группы спрайтов — специального контейнера для управления несколькими объектами (спрайтами),

  self.reset() создать 



   draw_grid()   рисует сетку на экране

     win.blit(...)
Эта строка рисует текст на экране.